1) only 1 page/1 HTML file
2) grid view of pokemon (centered, 3 per row)
    - include name, height, weight, species, growth rates, abilities, stats
3) search field at top (right?)
4) add them to collection, up to 6
5) delete from collection

to set up a json database
json-server --watch db.json
(instructs json-server to set up a db.json in current directory)

